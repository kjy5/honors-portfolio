---
import { withBasePath } from "../utils/path";

interface Props {
	path: string;
}

const { path } = Astro.props;
---

<embed
  src={withBasePath(path)}
  type="application/pdf"
  width="100%"
  height="600px"
/>
